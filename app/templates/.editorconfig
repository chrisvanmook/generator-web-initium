# editorconfig.org
root = true

# 2 space indentation
[*]
indent_style = space
indent_size = 2
trim_trailing_whitespace = true

# Set default charset
[*]
charset = utf-8

# Unix-style newlines with a newline ending
[*]
end_of_line = crlf
insert_final_newline = true
